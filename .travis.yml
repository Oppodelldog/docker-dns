language: go

go:
  - 1.x

os:
  - linux

env:
  global:
    - GO111MODULE=on
    - GOFLAGS="-mod=readonly"
    
go_import_path: github.com/Oppodelldog/docker-dns

services:
  - docker

script:
  - go mod edit -dropreplace "github.com/Oppodelldog/dockertest@v0.0.3"
  - go run .test/main.go